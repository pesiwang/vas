CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT(vas)

SET(CMAKE_CXX_FLAGS "-g -Wall -DDEBUG")

INCLUDE_DIRECTORIES(
    include
)

AUX_SOURCE_DIRECTORY(src/core SRC_LIST)
AUX_SOURCE_DIRECTORY(src/handler SRC_LIST)

ADD_LIBRARY(vas ${SRC_LIST})
